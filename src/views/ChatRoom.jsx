import React from 'react';

const ChatRoom = () => {
	React.useEffect(() => {
		// eslint-disable-next-line react-hooks/exhaustive-deps
	}, []);

	return (
		<div>
			<></>
		</div>
	);
};

export default ChatRoom;
